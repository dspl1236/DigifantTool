"""
version.py — Single source of truth for DigiTool version.

Versioning:
  MAJOR — milestone / breaking change
  MINOR — new feature or tab
  PATCH — bug fixes
"""

APP_VERSION  = "0.7.0"
APP_NAME     = "DigiTool"
WINDOW_TITLE = f"DigiTool  v{APP_VERSION}  —  Digifant 1 / 2 / 3  ECU Editor"
